{
  "name": "flash-docs",
  "version": "1.0.0",
  "description": "Flash project documentation site",
  "private": true,
  "scripts": {
    "build": "hugo --minify --ignoreErrors",
    "dev": "hugo server",
    "clean": "rm -rf public resources",
    "prebuild": "hugo mod get -u && hugo mod tidy",
    "pagefind": "pagefind --site public"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.22",
    "postcss-cli": "^11.0.1",
    "rtlcss": "^4.3.0"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@9.0.0"
}
